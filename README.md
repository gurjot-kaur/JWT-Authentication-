# JWT-Authentication

Run python authtest.py

Copy and save keys 

Run uvicorn authadd:app --reload --port 8000

Run uvicorn authmultiply:app --reload --port 8001

Run uvicorn authmultipyandadd:app --reload --port 8002
